#![allow(unused)]
pub fn parse(input: &str) -> usize {
    unimplemented!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_parse() {
        let input =
"test"
;
        assert_eq!(0, 0);
    }
}
